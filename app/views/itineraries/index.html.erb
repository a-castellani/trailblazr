<h1>All Itineraries</h1>

<div>
  <% @itineraries.each do |itinerary| %>
    <ul>
      <%= link_to itinerary.title, itinerary_path(itinerary) %>
    </ul>
  <% end %>
</div>

<div>
  <%= link_to "New itinerary", new_itinerary_path, class:"btn btn-primary text-align-center"%>
</div>
