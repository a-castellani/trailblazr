<div class="footer">
  <div class="d-flex">
    <h4>TrailBlazr</h4>
    <p>Contact Us</p>
  </div>
  <% if controller_name == 'itineraries' && action_name == 'show' %>
    <div class="chat">
    <%= render "itineraries/chat" %>
    <% end %>
</div>
</div>
