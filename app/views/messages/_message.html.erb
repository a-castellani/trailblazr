<div>
  <small>
    <strong><%= message.user.first_name %></strong>
    <em><%= message.created_at.strftime("%d/%m/%Y at %l:%M %p") %></em>
  </small>
  <div><p><%= message.content %></p></div>
</div>
