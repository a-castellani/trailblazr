<div class="message cont-<%= message.user.id %>">
  <div>
    <small class="name-<%= message.user.id %>"><strong><%= message.user.first_name %></strong></small>
    <em><%= message.created_at.strftime("%d/%m/%Y, %l:%M %p") %></em>
    <p class="content msg-<%= message.user.id %>"><%= message.content %></p>
  </div>
</div>
